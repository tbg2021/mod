political_decisions = {
    british_claim_on_ireland = {
        potential = {
            exists = IRE
            tag = BRI

            year = 1850

            NOT = { has_country_flag = british_claim_on_ulster }
            NOT = { has_global_flag = the_britains_reunified }
        }

        allow = {
            state_n_government = 1
        }

        effect = {
            set_country_flag = british_claim_on_ulster
            ENG_254 = { add_core = BRI }

            relation = {
                who = IRE
                value = -100
            }

            badboy = 2

            IRE = { country_event = 66649 }
        }

        ai_will_do = {
            factor = 1
        }
    }

    royal_treaty_of_dublin = {
        potential = {
            tag = CAN
            exists = IRE

            IRE = {
                NOT = { has_country_flag = ulster_abandoned }
            }

            NOT = { has_country_flag = royal_treaty_of_dublin }
            BRI = { has_country_flag = british_claim_on_ulster }
            NOT = { has_global_flag = the_britains_reunified }
        }

        allow = {
            IRE = { is_vassal = no }
        }

        effect = {
            set_country_flag = royal_treaty_of_dublin
            IRE = { country_event = 66650 }
            badboy = -2
            prestige = 5
        }

        ai_will_do = {
            factor = 1
        }
    }

    # TODO make these events rather than decisions. Similar to Boshin War.
    take_in_war_CAN = {
        potential = {
            tag = CAN
            NOT = {
                has_global_flag = the_britains_reunified
                has_global_flag = the_final_battle
            }

            war_with = BRI
            BRI = { owns = 300 }
        }

        allow = {
            controls = 300
            BRI = {
                war_exhaustion = 100
            }
        }

        effect = {
            end_war = BRI
            inherit = BRI
            set_global_flag = the_britains_reunified
            country_event = 66631
        }

        ai_will_do = {
            factor = 1
        }
    }

    take_in_war_BRI = {
        potential = {
            tag = BRI
            NOT = {
                has_global_flag = the_britains_reunified
                has_global_flag = the_final_battle
            }
            war_with = CAN
            CAN = { owns = 57 }
        }

        allow = {
            controls = 57
        }

        effect = {
            end_war = CAN
            inherit = CAN
            set_global_flag = the_britains_reunified
            country_event = 66630
        }

        ai_will_do = {
            factor = 1
        }
    }
}
